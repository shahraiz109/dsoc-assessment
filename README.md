# GitHub Repo Health Comparator

A small public API consumer that compares GitHub repositories side by side and highlights practical health signals.

## Run Locally

```bash
npm install
npm run dev
```

Open http://localhost:3000.

## Current Status

The initial Next.js project shell is ready. The comparison features will be added in small committed steps.
